version: "2"
sql:
- engine: "sqlite"
  queries: "db/query/"
  schema: "db/migrations/"
  gen:
    go:
      package: "db"
      out: "db/gen"
      emit_json_tags: true
      emit_pointers_for_null_types: true