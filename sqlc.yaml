version: "2"
sql:
- engine: "sqlite"
  queries: "db/sqlite/query/"
  schema: "db/sqlite/migrations/"
  gen:
    go:
      package: "db"
      out: "db/sqlite/gen"
      emit_json_tags: true
      emit_pointers_for_null_types: true